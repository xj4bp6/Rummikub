# 網頁版拉密（Rummikub）多人連線遊戲 - 完整架構設計與實作方案

本方案針對在 **GitHub Pages** 上免費部署、供朋友間跨平台（Windows / Android / iOS）遊玩的 **2~4 人私人房間拉密（Rummikub）遊戲** 進行完整系統規劃。

---

## 1. 系統架構與技術選型 (Architecture & Tech Stack)

為了實現「零成本託管（GitHub Pages）」以及「跨裝置（電腦與手機/平板）順暢體驗」，技術選型規劃如下：

### 1.1 前端技術棧 (Frontend)
* **核心框架**：`React` + `TypeScript` + `Vite`
  * 選擇 React 與 TypeScript 提供高度組件化與強型別防錯機制，維護複雜的牌桌與牌組 state。
* **樣式與 UI**：`TailwindCSS` + `Lucide Icons` + `Framer Motion`
  * 打造現代暗黑高質感 UI，支援卡牌拖拽與推擠動畫。
* **拖拽與觸控互動**：`@dnd-kit/core` 或「雙重操作模式（點擊選擇 + 拖拽）」
  * **重點相容性**：行動裝置（iOS Safari / Android Chrome）螢幕較小，單靠拖拽易誤觸，必須提供「**點擊卡牌 $\rightarrow$ 點擊目標區域**」與「**拖拽 (Drag & Drop)**」雙模支援。

### 1.2 網路連線與多人同步方案 (Networking & Sync)
針對 GitHub Pages 靜態託管的限制，提供兩種**零成本/低成本**連線方案比較：

| 方案 | 連線技術 | 優勢 | 劣勢 | 推薦程度 |
| :--- | :--- | :--- | :--- | :--- |
| **方案 A：P2P WebRTC (PeerJS)** | WebRTC 直連 (房主兼 Host) | 100% 免費、無伺服器流量限制、延遲最低 | 若房主關閉網頁房間即結束 | **首選推薦 (主方案)** |
| **方案 B：Firebase Realtime DB** | Cloud WebSocket | 斷線可重新加入、離線狀態紀錄簡單 | 需設定 Firebase 免費專案 | **備選方案** |

> **推薦採用【方案 A：PeerJS / WebRTC】**：
> 房主創建房間時生成 `Room ID`（即 Peer ID），其餘玩家輸入代碼加入。房主的瀏覽器作為權威端（Authoritative Host），負責洗牌、發牌、驗證回合合法性與廣播狀態，完全不需要付費後端伺服器！

---

## 2. 核心遊戲邏輯與演算法設計 (Core Game Logic)

### 2.1 資料結構設計 (Data Models)

```typescript
// 顏色類型
type Color = 'red' | 'black' | 'orange' | 'blue';

// 卡牌介面
interface Tile {
  id: string;          // 唯一識別碼 (例如: "red-7-1", "joker-1")
  color?: Color;       // 鬼牌可為 undefined
  value?: number;      // 1~13，鬼牌可為 undefined
  isJoker: boolean;    // 是否為鬼牌
}

// 牌桌組合 (Set)
interface TileSet {
  id: string;
  type: 'group' | 'run' | 'invalid';
  tiles: Tile[];
}

// 遊戲狀態 (Game State)
interface GameState {
  roomId: string;
  players: {
    id: string;
    name: string;
    hand: Tile[];
    hasMelded: boolean; // 是否已破冰
    isHost: boolean;
  }[];
  currentTurnIndex: number;
  drawPile: Tile[];      // 中央牌庫
  tableSets: TileSet[];  // 牌桌上的組合
  timer: number;         // 當前回合剩餘秒數 (預設 60 秒)
  turnSnapshot?: {       // 回合快照 (用於 Rollback)
    hand: Tile[];
    tableSets: TileSet[];
  };
}
```

### 2.2 牌組合法性驗證引擎 (Set Validation Engine)

1. **群組（Group / 同數不同色）驗證**：
   * 長度必須為 3 或 4。
   * 排除鬼牌後，所有實體牌的數字必須**相同**。
   * 排除鬼牌後，所有實體牌的顏色必須**完全不重複**。
   * 鬼牌自動補足缺失的顏色。

2. **順子（Run / 同色連續數）驗證**：
   * 長度必須 $\ge 3$。
   * 排除鬼牌後，所有實體牌的顏色必須**相同**。
   * 實體牌按數字排序後，非鬼牌間的數字間隔必須能被鬼牌數量完全填補。
   * 數字不循環（13 後不能接 1）。

3. **鬼牌（Joker）動態數值解析**：
   * 在進行破冰 30 分計算與替換鬼牌時，演算法會自動計算當前組合中鬼牌所「代表」的精確顏色與數字。

### 2.3 破冰規則（Initial Meld）邏輯
* **條件檢查**：
  * 玩家 `hasMelded === false` 時發起破冰。
  * **規則限制**：玩家本回合打出的組合，**只能包含玩家手牌**，不得將牌桌上已有卡牌拉入組合。
  * **點數計算**：本回合打出的所有新組合分數總和 $\ge 30$ 分（鬼牌依其替代之數字計算）。
* **通過破冰**：將 `hasMelded` 設為 `true`，並更新牌桌與手牌。

### 2.4 回合快照與復原機制 (Snapshot & Rollback)
* **回合開始**：備份 `turnSnapshot = { hand: [...hand], tableSets: [...tableSets] }`。
* **回合結束觸發**（玩家點擊「結束回合」）：
  1. 檢查牌桌上**所有組合**是否皆為 `valid`（長度 $\ge 3$ 且符合 Group 或 Run）。
  2. 檢查玩家本回合是否**至少打出 1 張手牌**。
  3. 若驗證成功，發送廣播更新狀態，進入下一位玩家回合。
* **違規 / 超時 / 主動重置 (Rollback)**：
  1. 若驗證失敗、回合 60 秒計時結束、或玩家主動點「重置」：
  2. 牌桌與手牌立即還原為 `turnSnapshot`。
  3. 若為超時或無效結束，強制玩家從中央牌庫抽 1 張懲罰牌，並結束回合。

---

## 3. 使用者介面與跨平台體驗設計 (UI/UX & Mobile Design)

### 3.1 畫面區塊佈局 (Layout)

```
+-------------------------------------------------------------------+
| 房間代碼: #8821 | 當前玩家: 小明 (00:45) | 中央牌庫: 48 張 | [離開]   |
+-------------------------------------------------------------------+
| [其他玩家面板]                                                      |
|  * 玩家 A (手牌 12 張 | 已破冰)   * 玩家 B (手牌 14 張 | 未破冰)       |
+-------------------------------------------------------------------+
| 中央牌桌 (Table Board) - 支援滾動與動態區域                         |
|  +-------------------+  +--------------------------------+        |
|  | [紅7] [黑7] [藍7] |  | [藍4] [藍5] [藍6] [鬼牌(藍7)]   |        |
|  +-------------------+  +--------------------------------+        |
|  +-------------------+  +--------------------------------+        |
|  | [橙10][橙11][橙12]|  | [點擊/拖拽區域 新建組合 +]     |        |
|  +-------------------+  +--------------------------------+        |
+-------------------------------------------------------------------+
| 我的手牌區 (Player Rack)                                           |
|  [紅3] [藍3] [橙5] [黑6] [藍9] [紅10] [鬼牌]                        |
|                                                                   |
|  [排序：依顏色] [排序：依數字] | [復原本回合] [摸牌/過] [結束回合]   |
+-------------------------------------------------------------------+
```

### 3.2 移動端（iOS / Android）適配優化
1. **點擊選取模式 (Tap-to-Select)**：
   * 點擊手牌/桌上的卡牌選中（高亮外框）。
   * 點擊目標牌組或「新建組合」區域自動放入。
   * 解決手機畫面上用手指拖動小卡牌遮擋視線的問題。
2. **手牌自動整理**：
   * 提供「按顏色排序」（方便找同色順子）與「按數字排序」（方便找同數群組）一鍵按鈕。
3. **回應式尺寸 (Responsive Design)**：
   * 卡牌大小使用 `clamp()` 與 `vw/vh` 單位，確保在手機豎屏/橫屏及平板上都能清晰顯示。

---

## 4. 部署與開發流程 (Deployment & Roadmap)

### 4.1 GitHub Pages 部署流程
* 使用 GitHub Actions 自動化部署：
  1. 當推送到 `main` 分支時，觸發 GitHub Action。
  2. 執行 `npm run build` 生成 `dist` 靜態檔案。
  3. 自動發布至 `gh-pages` 分支，免費產生 `https://<username>.github.io/rummikub` 連結。

### 4.2 開發階段規劃 (Roadmap)

* **階段一：基礎核心與牌桌邏輯 (Core Logic & Engine)**
  * [ ] 建立 Vite + React + TypeScript 專案。
  * [ ] 完成牌組生成、洗牌、發牌資料結構。
  * [ ] 實作組合驗證演算法（Group、Run、Joker 替換）。
  * [ ] 實作單機模式（或測試用 2 人本地對戰），驗證破冰（$\ge 30$ 分）與 Snapshot/Rollback 機制。

* **階段二：UI 與互動體驗 (UI & Interaction)**
  * [ ] 繪製質感牌桌與卡牌組件（現代暗黑遊戲風格）。
  * [ ] 實作卡牌拖拽（Dnd-Kit）與點擊選取（Tap-to-Select）雙互動邏輯。
  * [ ] 實作手牌自動排序（依顏色 / 依數字）與回合倒數計時器。

* **階段三：WebRTC 多人連線與房間系統 (Multiplayer)**
  * [ ] 整合 PeerJS，實現創建房間（生成 Room ID）與加入房間。
  * [ ] 實作 Host 權威端同步機制（發牌、回合切換、廣播牌桌狀態）。
  * [ ] 處理連線異常與玩家斷線提示。

* **階段四：測試、修飾與 GitHub Pages 發布 (Polish & Deploy)**
  * [ ] 手機端（iOS Safari / Android Chrome）與電腦端實機響應式測試。
  * [ ] 設定 GitHub Actions 靜態網頁自動部署。
  * [ ] 撰寫遊戲教學與操作說明 Modal。

---

## 5. 針對用戶回饋的精確規格調整 (Updated Specs Based on User Feedback)

根據您的寶貴回饋，我們將系統規格更新如下：

### 5.1 GitHub 儲存庫與發布 URL
* **GitHub Repository**: `https://github.com/xj4bp6/Rummikub`
* **託管部署**: 設定 GitHub Actions 自動發布至 GitHub Pages (靜態網頁，完全免費且不限流量)。

### 5.2 四邊玩家視角與座位轉動機制 (4-Side Table & Perspective Lock)
* **「自己」永遠在螢幕下方**：
  * 無論目前輪到誰的回合，當前觀看畫面的玩家，自己的手牌架與頭像永遠固定在螢幕正下方（Bottom）。
* **四邊順時針座次排列**：
  * 視角會根據目前玩家的座位索引進行「相對旋轉」顯示：
    * **下方 (Bottom)**：自己 (Current Local Player)
    * **左側 (Left)**：順時針下一位玩家 (Turn + 1)
    * **上方 (Top)**：對面玩家 (Turn + 2，適用於 3~4 人局)
    * **右側 (Right)**：順時針上一位玩家 / 順位 3 (Turn + 3，適用於 4 人局)
  * 回合指示燈與發牌順序將依此四邊順序流暢輪替。
* **房主房間設定：玩家發牌順序**：
  * `依加入順序 (Join Order)` (預設)：依照進入房間的 1, 2, 3, 4 順序決定行動順序。
  * `隨機抽籤 (Random Order)`：遊戲開始時，系統隨機洗牌決定玩家行動順序。

### 5.3 鬼牌 (Joker) 計分與破冰規則澄清
* **破冰（Initial Meld）計分**：
  * **鬼牌可參與破冰**：鬼牌在破冰組合中，會依其所在位置的「實際替代數值」進行點數累加。
  * **位置計分範例**：
    * `[鬼牌, 藍9, 藍10]` $\rightarrow$ 鬼牌代表 `藍8` $\rightarrow$ 點數算作 $8 + 9 + 10 = 27$ 分（未達 30 分，不可破冰）。
    * `[藍9, 藍10, 鬼牌]` $\rightarrow$ 鬼牌代表 `藍11` $\rightarrow$ 點數算作 $9 + 10 + 11 = 30$ 分（達 30 分，可成功破冰）。
    * `[紅10, 藍10, 鬼牌]` $\rightarrow$ 鬼牌代表 `10` $\rightarrow$ 點數算作 $10 + 10 + 10 = 30$ 分（達 30 分，可成功破冰）。
* **結算時鬼牌點數懲罰（30 分）**：
  * 當中央牌庫抽完且無人能再出牌時，遊戲進入點數結算。
  * 結算時計算每位玩家手上**剩餘未打出的手牌點數總和**。
  * 一般數字牌計為其數字點數（1~13 分）；**若手牌中仍留有鬼牌，該張鬼牌計為 30 點懲罰分**。
  * 最終手牌總點數最低者獲勝。

### 5.4 房主自訂思考時間選項 (Custom Turn Timer)
* 創建房間時，房主可自由選擇每回合思考時間上限：
  * `30 秒`（極速對戰）
  * `45 秒`
  * `60 秒`（標準預設）
  * `90 秒`（輕鬆思考）
  * `120 秒`
  * `不限時`（無倒數，適合朋友閒聊慢慢想）

---

請確認以上更新規格。確認無誤後，我們即可開始建立 Vite + React + PeerJS 基礎專案架構並推進開發！

